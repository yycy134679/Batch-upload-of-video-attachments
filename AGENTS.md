- 安装依赖、测试和运行脚本必须先进入虚拟环境(.venv)。
- 向用户提问前，必须先阅读代码，理解核心逻辑和功能。

## Git 开发规则

除非改动属于低风险例外，否则不要直接在 `main` 分支上修改代码。

默认做法：
- 开始任何新功能、非极小 bug 修复、重构，先从最新 `main` 创建工作分支。
- 分支命名尽量清晰，优先使用 `feature/中文描述`、`fix/中文描述`、`refactor/中文描述`、`chore/中文描述` 的形式；类别前缀保留英文，斜杠后的具体任务描述可以使用中文。
- 在工作分支上完成修改、自检和必要测试后，再建议我合并回 `main`。
- 若开发周期较长，开始前或合并前应先同步最新 `main`，避免分支漂移和冲突。
- 涉及多个独立任务时，不要混在同一分支，按任务拆分分支。

可直接在 `main` 修改的低风险例外：
- 纯文档改动
- 很小的配置改动
- 明显局部、低影响、易回滚的极小修复

即使属于例外，也应先判断是否真的低风险；只要改动会影响业务逻辑、公共接口、数据结构、依赖、构建、测试或范围不够确定，就不要直接改 `main`，应改用工作分支。

在执行 git 相关操作时，遵循以下原则：
- 若当前在 `main` 且任务不属于低风险例外，先建议或执行创建分支，再开始改代码。
- 在未确认改动稳定前，不要建议直接合并到 `main`。
- 完成改动后，应说明本次改动适合走分支合并，还是可作为低风险例外直接保留在 `main`。
- 优先保持 `main` 可用、干净、易回滚。